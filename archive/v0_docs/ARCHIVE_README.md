# 归档说明

本目录保存 v0 基线文档。其中一部分从旧工作区物理目录 `v1_docs/` 原样迁入；该物理名称现在仅作为来源追踪的历史旧称，不再表示仓库代际。`repository-artifacts/` 保存核心 ZIP 中原位于 `C_Semantic_Treehouse/docs/` 的文档。两类内容均已冻结，不再维护；一个含个人绝对路径的 Phase 8 文档按公开策略排除并记录在 provenance 中。
