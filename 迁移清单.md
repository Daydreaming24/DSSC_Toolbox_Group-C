# DSSC C 组 v0.4 仓库迁移清单

> 状态更新：2026-08-14。Phase 00–08 主线为 `COMPLETE`。维护者（GitHub 身份 `Daydreaming24`）已明确接受 P00-R14 的最终人工治理责任，该风险终态为 `ACCEPTED_LIMITATION`，当前无 `OPEN_BLOCKING` 风险。已确认候选具有 Phase 09 §6.9–§6.11 的完整核验记录；每个发生 tracked 内容变化的新候选均须独立完成同一技术链。有效状态以 [`docs/v0.4/STATUS.md`](docs/v0.4/STATUS.md) 的最新追加记录为准，动态事实见 [`publication-record.md`](docs/v0.4/publication-record.md)。
>
> v0.4 已公开托管于 [`Daydreaming24/DSSC_Toolbox_Group-C`](https://github.com/Daydreaming24/DSSC_Toolbox_Group-C)，当前不授予通用 license；该状态为已接受限制。tag、GitHub Release、branch protection 和 default-branch 更改为 `NOT_REQUESTED`。Semantic Treehouse 为隔离可选轨：受控部署/导入/SHACL 有本地证据，`current runtime=PAUSED`，`publication=NOT RUN`；raw preflight 历史结论保持 `BLOCKED`。Mermaid render 与外部 ITB/SEMIC 保持 `DEFERRED`。

## 1. 状态真源与命名边界

- 已完成历史：[`docs/v0.4/STATUS.md`](docs/v0.4/STATUS.md)。
- 当前中断：[`docs/v0.4/CHECKPOINT.md`](docs/v0.4/CHECKPOINT.md)。
- 发布风险收口：[`docs/v0.4/release-readiness.md`](docs/v0.4/release-readiness.md)。
- 人工决定：[`docs/v0.4/human-decisions.md`](docs/v0.4/human-decisions.md)。
- 发布动作记录：[`docs/v0.4/publication-record.md`](docs/v0.4/publication-record.md)。
- release/artifact/hash：[`release-manifest.json`](C_Semantic_Treehouse/manifests/release-manifest.json)。
- suite/version/composition：[`validation-suites.json`](C_Semantic_Treehouse/manifests/validation-suites.json)（`contract_version=1.6.0`）。
- 最终 deliverables：[`deliverables.json`](C_Semantic_Treehouse/manifests/deliverables.json)。
- D 组 requirements 与 fixtures：[`v0.4-requirements.json`](C_Semantic_Treehouse/manifests/v0.4-requirements.json) 和 [`v0.4-test-cases.json`](C_Semantic_Treehouse/manifests/v0.4-test-cases.json)。

名称保持以下边界：`v0` 是原仓库整体迁移基线；`v0.1`–`v0.3` 是历史模型版本；`v0.4` 是当前模型版本；Dataset ID `building-energy-hourly-v1` 保持原值。v0.4 metadata 的兼容性分类是 `wire-profile-breaking`，Energy Reading Record 继续精确复用 v0.3 合同。

## 2. 来源、仓库与冻结边界

- [x] 两份原始 ZIP 原样进入 `inputs/source-archives/received/`，纳入 frozen manifest。
- [x] 原始任务计划清理后的 11 个有效文件进入 `inputs/original-plan/`，macOS 元数据留在 ZIP 原件内。
- [x] D 组 TTL 与修改说明原样进入 `inputs/d-group/v0.4/received/`；TTL 是规范性可执行契约。
- [x] 项目要求进入 `inputs/project/v0.4/`。
- [x] v0 文档、summaries、validation reports 和 prompts 分别进入 `archive/` 与 `prompts/v0/`，保持历史只读。
- [x] v0.1–v0.3 模型保持原字节并进入永久保护范围。
- [x] 新仓库拥有独立 Git 历史，没有导入旧 `.git/`。
- [x] frozen manifest 登记的 104 个文件已由 Phase 00–09 本地校验保持 104/104。
- [x] Phase 09 deliverables / publication-safety / evidence-freshness 已建立并本地通过（稳定路径见 release evidence）。
- [x] Phase 09 内容决定：D 组/来源 ZIP 再分发与历史路径公开风险已批准；公开托管且无通用 license grant 为已接受限制。

稳定证据：[`STATUS.md`](docs/v0.4/STATUS.md) 的 `## Phase 00 — 当前状态复核、范围冻结与执行台账`，以及 [`source-archives.md`](docs/provenance/source-archives.md)。

## 3. 环境与统一入口

- [x] CPython 3.12.10、pip/bootstrap toolchain、runtime dependencies 和两份 hash locks 已固定。
- [x] Windows AMD64 的 bootstrap、repo `.venv`、doctor、PowerShell 5.1 wrapper 和路径健壮性已实测。
- [x] 固定 `linux/amd64` 容器使用 pinned Python image digest、non-root、read-only root、`network_mode: none` 和窄 evidence mounts。
- [x] Windows、Linux、Make 与 Docker 入口汇入唯一 `scripts/validate.py` Python dispatcher。
- [x] 七个公开 suite 名称固定为 `frozen`、`environment`、`baseline`、`traceability`、`v0.4-model`、`v0.4`、`all`。
- [x] 缺依赖、lock/hash 漂移、未知 entrypoint、空/重复 composition 和 required skip 均 fail closed。
- [x] WSL2 Linux host 从 bootstrap 到 `all` 的端到端认证：Phase 08 已在 Ubuntu 24.04.4 LTS / x86_64 实测；独立 bare-metal Linux 仍为 `UNTESTED/DOCKER_FALLBACK`。
- [x] 使用冻结 registry 重建镜像并运行 Docker `all`：Phase 08/09 本地固定 Docker 轨已实测（17/17）。
- [x] Windows/Linux/Docker GitHub Actions **实际**运行：最近已确认候选绑定的 Ubuntu/Windows/Docker 三个必需 job 全部成功；最新 run URL 与结论见 [`publication-record.md`](docs/v0.4/publication-record.md)。
- [x] Windows/Linux reproduce wrappers 与本地 clean-room rehearsal：Phase 08 已实测；Phase 09 只读复核 reproduce 入口。
- [x] 最近已确认候选的 Windows/Linux local clean clone 与从 canonical GitHub URL 的 remote clean clone 均完成一键复现和最终 QA。

稳定证据：[`STATUS.md`](docs/v0.4/STATUS.md) 的 Phase 01、Phase 08、Phase 09 小节；使用说明见 [`docs/environment.md`](docs/environment.md) 与 [`scripts/README.md`](scripts/README.md)。

## 4. v0.1–v0.3 baseline

- [x] [`baseline-test-cases.json`](C_Semantic_Treehouse/manifests/baseline-test-cases.json) 与 schema 已建立并通过跨记录语义校验。
- [x] 33/33 mandatory cases 在 Windows host 与固定 Linux container 通过，规范化语义结果一致。
- [x] RDF、JSON-LD、SHACL 正反例、JSON Schema、OpenAPI 和历史 8 个 SPARQL CQ 全部由 manifest 驱动。
- [x] 37 项 baseline 负控及 30 项 registry/output/Docker 合同负控通过。
- [x] 历史 v0 PASS 与当前运行证据已分开叙述。

稳定证据：[`STATUS.md`](docs/v0.4/STATUS.md) 的 `## Phase 02 — v0.1–v0.3 baseline reproduction（COMPLETE）`；已审核发布证据见 [`baseline/README.md`](C_Semantic_Treehouse/evidence/releases/v0.4/baseline/README.md)。

## 5. D 组追踪、兼容性与 v0.4 模型

- [x] D04-R001–D04-R017 已进入 requirements manifest 和人读追踪表。
- [x] D 组 TTL 的 Shape/path/severity/message 与 requirements 完成双向覆盖。
- [x] ADR-001、ADR-002、ADR-003 已批准，覆盖 `dct:conformsTo`/Closed Shape、wire profile identity 与 record inheritance。
- [x] `model/v0.4/` 已从冻结 D 组输入派生 ontology、context、Shape、canonical JSON-LD、README 与 `SHA256SUMS`。
- [x] release manifest 已登记 v0.1–v0.4 artifacts、hash、origin、applicable validators 与五项 v0.3 record inheritance。
- [x] v0.4 model 在 Windows host 和固定 Linux container 通过 schema、path、hash、JSON-LD、SHACL/meta-SHACL 与 provenance 校验。
- [x] v0.3 → v0.4 metadata 明确分类为不兼容 wire-profile；Dataset ID 保持 `building-energy-hourly-v1`。

稳定证据：[`STATUS.md`](docs/v0.4/STATUS.md) 的 `## Phase 03 — D 组契约审计、需求追踪与兼容性决策（COMPLETE）` 和 `## Phase 04 — v0.4 派生模型与统一 Release Manifest（COMPLETE）`；已审核模型证据见 [`model/README.md`](C_Semantic_Treehouse/evidence/releases/v0.4/model/README.md)。

## 6. 四状态 harness、语义测试、质量与治理

- [x] 66 个独立 fixture/case 已登记：PASS 6、FAIL 53、INAPPLICABLE 1、UNTESTABLE 6。
- [x] 66/66 expected/actual 匹配，60 个 report cases 完成 467 条结构化 oracle 断言。
- [x] 四状态优先级和 `SUCCESS/ERROR` 程序状态已实现；零发现、零执行、required skip 和 authority/harness 故障非零退出。
- [x] 20/20 版本绑定 SPARQL CQ 通过：历史 v0.3 8 个，v0.4 12 个。
- [x] 八类 quality 指标从 manifests 与实际图计算；requirement 自动测试覆盖 17/17，四状态覆盖 4/4。
- [x] SSSOM 累积 47 行并全部标记 `PENDING_DOMAIN_REVIEW`。
- [x] 七类 governance/provenance 文件通过 21/21 checks、五-manifest preflight、负控和确定性校验。
- [x] Phase 06 `all` 在 Windows host 以 16/16 components、0 failed、0 skipped 退出 0。
- [x] 维护者明确接受 C Group final semantic review、D Group final contract review、Domain Reviewer、47 条 SSSOM domain review 与 Release Approver 的最终责任（P00-R14 / `ACCEPTED_LIMITATION`）；早期 `PENDING` 状态继续保留。

稳定证据：[`STATUS.md`](docs/v0.4/STATUS.md) 的 `## Phase 05 — 四状态 Fixtures 与 Fail-Closed 验证 Harness（COMPLETE）` 与 `## Phase 06 — 语义测试、质量指标与治理（COMPLETE）`。

## 7. Phase 07 文档、图源与 handoff 收口

以下 Phase 07 required gates 已通过。documentation canonical、negative controls、deterministic rerun、registry/release binding 和最终 `all` 共同形成完成证据。

- [x] 根 README、迁移清单、package README、package scripts README 与本目录导航通过 documentation check。
- [x] 四份 C 组核心报告与 manifests/results 一致。
- [x] 两张 Mermaid source 通过结构 lint；该结果只表示结构检查。
- [x] A/B/D handoffs 的字段、hash、迁移、命令、report 解读和边界通过检查。
- [x] AI-assisted human-governed 文档明确人工与 machine gates。
- [x] documentation checker self-test 的 broken link、绝对路径、未知 suite/artifact、manifest mismatch、zero discovery、Mermaid 缺边和虚假完成状态等 46/46 负控全部非零拒绝。
- [x] suite registry 推进至 contract version `1.6.0`，documentation required component 已纳入 `all`，release binding 一致。
- [x] Windows host documentation normal/deterministic run、`all` 17/17、frozen 104/104 和最终 diff 审计全部退出 0。
- [x] 本轮最终交接动作已纳入：主代理在最终复验后向 [`STATUS.md`](docs/v0.4/STATUS.md) 追加 Phase 07 `COMPLETE` 小节，并将 `CHECKPOINT.md` 恢复空闲。

Phase 07 机器结果位于仓库根的 build/validation/documentation/ 与 build/phase-07/；canonical 为 15 个发现对象、19/19 checks PASS，negative controls 为 46/46。registry `1.6.0` 的当前 SHA-256 与 release manifest 绑定；`all` 为 17/17 components PASS。

## 8. Phase 08 跨平台与可选轨

- [x] Windows / WSL2 Linux / 固定 Docker / clean-room / CI 静态合同：Phase 08 required core `COMPLETE`。
- [x] Semantic Treehouse 固定版本与 bounded checkout/materialization：`v4.3.0` / `e6d7315a09afdfaadbe3ad1a09cf5305f8d13faf`。
- [x] Semantic Treehouse raw preflight 与人工风险决定：raw 保持 `BLOCKED`；finding-specific opt-in 已批准。
- [x] Semantic Treehouse `PrepareOnly` 边界已核验。
- [x] Semantic Treehouse recovery：受控 image build/deployment/workload/migration/UI/import/export/SHACL 正负控；当前 `current runtime=PAUSED`。
- [ ] Semantic Treehouse publication：`NOT RUN`。
- [ ] Mermaid parser、renderer、渲染成功和视觉 QA：`DEFERRED`。
- [ ] 外部 SEMIC/ITB validator：`DEFERRED` / `NOT RUN`。

稳定证据：`STATUS.md` Phase 08 及 recovery addenda；核心 `all` 不依赖可选轨。

## 9. Phase 09 最终 QA 与发布门槛

### 9.1 已完成（稳定本地门槛）

- [x] §6.1 validation-suites 合同冻结与负控（与 Phase 08 hash 一致）。
- [x] §6.2 `deliverables.json` / schema / `check_deliverables.py`。
- [x] §6.3 `check_publication_safety.py` / `check_evidence_freshness.py` 与 scanner 负控。
- [x] §6.4 只读复核 `reproduce.ps1` / `reproduce.sh`。
- [x] §6.5 本地 host + Docker `all` 17/17 与 documentation/CI checker。
- [x] §6.6 无自引用 core-results / core-report / evidence-index。
- [x] §6.7 稳定最终文档（README、迁移清单、final checklist、demo、FINAL_SUMMARY、release-readiness、human-decisions、publication-record、docs/scripts README）。

稳定证据路径（不写临时 `build/` 为唯一真源）：

- [`C_Semantic_Treehouse/evidence/releases/v0.4/`](C_Semantic_Treehouse/evidence/releases/v0.4/)
- [`C_Semantic_Treehouse/manifests/deliverables.json`](C_Semantic_Treehouse/manifests/deliverables.json)
- [`docs/v0.4/release-readiness.md`](docs/v0.4/release-readiness.md)
- [`C_Semantic_Treehouse/docs/final-checklist.md`](C_Semantic_Treehouse/docs/final-checklist.md)

### 9.2 最近已确认候选（发布链事实）

- [x] §6.8 再分发、隐私、提交身份、公开 repository 模式与逐项外部写授权；无通用 license grant 作为已接受限制。
- [x] 最近已确认候选完成 §6.9 candidate commit 与 Windows/Linux local clean clone 一键复现。
- [x] 最近已确认候选完成 §6.10 普通 push、候选绑定的 GitHub Actions 三个必需 job（Ubuntu/Windows/Docker）与远程 clean clone。
- [x] §6.11 记录性提交将最近已确认候选的 SHA、run URL/结论与 clone 结果写入 [`STATUS.md`](docs/v0.4/STATUS.md) 和 [`publication-record.md`](docs/v0.4/publication-record.md)。

### 9.3 最终人工治理责任

- [x] 维护者（GitHub 身份 `Daydreaming24`）明确接受 C/D final review、Domain Reviewer、47 条 SSSOM domain review 与 Release Approver 责任（P00-R14；`DEC-P09-P00-R14-RESPONSIBILITY-ACCEPTED`）。

P00-R14 终态为 `ACCEPTED_LIMITATION`，当前无 `OPEN_BLOCKING` 风险。每个发生 tracked 内容变化的新候选均须独立完成 Phase 09 §6.9–§6.11；有效状态以 [`STATUS.md`](docs/v0.4/STATUS.md) 的最新追加记录为准。tag、GitHub Release、branch protection 和 default-branch 更改均为 `NOT_REQUESTED`。

**不**在本清单填写当前 commit SHA、run ID/URL 或 ignored `build/` 临时路径作为“已发布”证明。

## 10. 当前完成标准对照

- [x] 两个原始 ZIP 和 104 项 frozen 记录可追溯。
- [x] v0.1–v0.3 baseline 33/33 无回归。
- [x] v0.4 requirements、模型、66-case 四状态 harness、SPARQL、quality 与 governance 有当前机器证据。
- [x] Phase 07 documentation gate 和冻结后的 `all` 17/17 有完成证据。
- [x] WSL2 Linux host、固定 Docker `all` 与 CI 静态合同有 Phase 08/09 本地证据。
- [x] 最近已确认候选的实际 GitHub Actions 三个必需 job 通过。
- [x] 最近已确认候选已从 canonical GitHub URL 全新 clone，并按最终 README 稳定复现。
- [x] 再分发、隐私、身份、公开 repository 与普通 push 决定均已落实；无通用 license grant 为已接受限制。
- [x] P00-R14 最终人工治理责任由维护者明确接受；早期产物 `PENDING` 状态与后续逐项签字责任保留。

v0.4 的本地语义治理核心、跨平台 required 轨与公开托管均已有证据；已确认候选的 CI 与远程 clean clone 记录可核查。每个发生 tracked 内容变化的新候选均须独立完成 §6.9–§6.11；有效状态以 `STATUS.md` 最新追加记录为准。当前无 `OPEN_BLOCKING` 风险。

## 11. 阶段记录摘要

| 日期 | Phase | 已证实结果 | 稳定记录 |
|---|---|---|---|
| 2026-08-09 | Phase 00 | 来源、104 项 frozen、范围和状态合同 | `STATUS.md` Phase 00 小节 |
| 2026-08-09 | Phase 01 | CPython 3.12.10、hash locks、Windows host 与固定 Linux container 环境 | `STATUS.md` Phase 01 及恢复小节 |
| 2026-08-09 | Phase 02 | v0.1–v0.3 baseline 33/33，两轨一致 | `STATUS.md` Phase 02 小节 |
| 2026-08-09 | Phase 03 | D 组契约追踪、17 requirements、3 ADRs | `STATUS.md` Phase 03 小节 |
| 2026-08-10 | Phase 04 | v0.4 model、release manifest、host/container model evidence | `STATUS.md` Phase 04 及恢复小节 |
| 2026-08-10 | Phase 05 | 66-case 四状态 harness、report assertions 与 fail-closed 负控 | `STATUS.md` Phase 05 小节 |
| 2026-08-10 | Phase 06 | 20 SPARQL CQ、八类质量指标、governance/provenance | `STATUS.md` Phase 06 小节 |
| 2026-08-10 | Phase 07 | 文档、图源、handoff、AI 治理、registry 1.6.0、`all` 17/17 | `STATUS.md` Phase 07 小节 |
| 2026-08-11 | Phase 08 | 跨平台、CI 静态、clean-room；Treehouse 可选轨 | `STATUS.md` Phase 08 小节 |
| 2026-08-12 | Phase 08 recovery | Treehouse 部署/导入/SHACL/`PAUSED`；documentation clean-room allowlist | `STATUS.md` Phase 08 addenda |
| 2026-08-12 | Phase 09 §6.1–§6.7 | 最终 QA checkers、core evidence、稳定文档与风险收口 | `STATUS.md` Phase 09 小节 |
| 2026-08-14 | Phase 09 人工治理恢复 | 维护者明确接受 P00-R14 最终责任；风险收口为 `ACCEPTED_LIMITATION`；发生 tracked 内容变化的新候选独立执行 §6.9–§6.11 | `STATUS.md` 最新追加记录、`human-decisions.md` 与 `publication-record.md` |
