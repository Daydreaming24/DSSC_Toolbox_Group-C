# 输入材料

本目录只保存项目输入，不保存生成报告。

- `source-archives/received/`：两份原始 ZIP 的字节级冻结副本。
- `original-plan/`：从原始任务计划 ZIP 去除包装目录和 macOS 元数据后得到的有效内容。
- `project/v0.4/`：本轮 C 组工作要求。
- `d-group/v0.4/received/`：D 组 v0.4 外部交付原件。

这些文件均按冻结输入管理；变更应通过新增版本目录或派生产物完成。原始 ZIP 不得重新压缩或原地替换。
